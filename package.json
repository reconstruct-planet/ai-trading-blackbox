{
  "name": "ai-trading-blackbox",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node server.mjs",
    "build": "node --check server.mjs && node --check app.js",
    "test": "npm run build"
  }
}
